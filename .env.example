# Backend API root (include the /api/v1 prefix if your backend uses it).
# On Vercel: Project → Settings → Environment Variables → add VITE_API_BASE_URL
# for Production (and Preview if needed), pointing at your live API; redeploy after changes.
VITE_API_BASE_URL=http://localhost:3000/api/v1
